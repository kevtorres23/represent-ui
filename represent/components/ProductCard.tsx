import { View, Text } from "react-native";

type productProps = {
    imageSrc: string,
    name: string,
    price: number,
} 

function ProductCard(props: productProps) {
    return(
        <View>
            
        </View>
    )
}

export default ProductCard;